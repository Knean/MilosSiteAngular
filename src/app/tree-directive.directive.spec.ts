import { TreeDirectiveDirective } from './tree-directive.directive';

describe('TreeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
